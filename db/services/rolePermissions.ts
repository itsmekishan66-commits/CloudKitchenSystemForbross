// import { asc, eq } from "drizzle-orm";
// import { db } from "@/db";
// import { roles, users, type NewUser } from "@/db/schemas";
// import {rolePermissions} from "@/db/schemas/rolePermissions";

// export async function createRolePermissions(role_id: number, permission_ids: number[]) {
// const result = await db.insert(roles).values(role_id);

// }
